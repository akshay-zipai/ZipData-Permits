# Prompt overrides

Place custom system prompt files here to override defaults:

- `permit_qa.txt`  — overrides the permit Q&A system prompt
- `reno_system.txt` — overrides the renovation system prompt

These are optional. If not present, the agent uses its built-in prompts.
